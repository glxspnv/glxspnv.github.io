const fs=require('fs'),vm=require('vm'),assert=require('assert/strict'),{parse}=require('../story-engine');
function element(){return {innerHTML:'',textContent:'',dataset:{},style:{},classList:{toggle(){},add(){},remove(){}},setAttribute(){},append(){},addEventListener(){},querySelectorAll(){return []},querySelector(){return null},scrollTo(){},clientWidth:900,clientHeight:700};}
for(const name of ['yeppi-rat','bitter-kim']){const p=parse(fs.readFileSync(`stories/${name}.txt`,'utf8'),`stories/${name}.txt`),els={};const context={currentStory:p,nodes:p.nodes,positions:p.positions,document:{querySelector:s=>els[s]??=element(),querySelectorAll:()=>[],createElementNS:element},window:{innerWidth:1300,addEventListener(){}},console};vm.createContext(context);vm.runInContext(fs.readFileSync('app.js','utf8'),context);assert.match(els['#reader'].innerHTML,/SCENE 00/);for(const n of Object.values(p.nodes)){vm.runInContext(`select(${JSON.stringify(n.id)})`,context);assert(els['#reader'].innerHTML.includes(n.title));}vm.runInContext('restart()',context);assert.match(els['#reader'].innerHTML,/SCENE 00/);if(name==='bitter-kim'){vm.runInContext('select(11)',context);assert.match(els['#reader'].innerHTML,/60%/);assert.match(els['#reader'].innerHTML,/40%/);}}
console.log('PASS: every scene renders, weighted choices render, restart always returns to first scene');
